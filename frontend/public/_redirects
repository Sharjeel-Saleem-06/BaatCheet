# Netlify redirects for SPA and API proxy

# API proxy to HuggingFace backend
/api/*  https://sharry121-baatcheet.hf.space/api/:splat  200!

# SPA fallback - all routes to index.html (200! means force)
# Note: Static files in public folder are served first automatically
/*    /index.html   200

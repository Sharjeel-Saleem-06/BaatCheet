# Netlify Configuration for BaatCheet Frontend
# NOTE: If this doesn't work, go to Netlify Dashboard > Site Settings > Build & Deploy
# and set: Base directory = frontend, Build command = npm install && npm run build, Publish = frontend/dist

[build]
  # CD into frontend and build there
  command = "cd frontend && npm install && npm run build"
  # Publish from frontend/dist
  publish = "frontend/dist"

[build.environment]
  NODE_VERSION = "18"

# Single Page Application redirect - all routes to index.html
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200

# API proxy to HuggingFace backend  
[[redirects]]
  from = "/api/*"
  to = "https://sharry121-baatcheet.hf.space/api/:splat"
  status = 200
  force = true

# Headers for security and performance
[[headers]]
  for = "/*"
  [headers.values]
    X-Frame-Options = "DENY"
    X-Content-Type-Options = "nosniff"
    Referrer-Policy = "strict-origin-when-cross-origin"

[[headers]]
  for = "/assets/*"
  [headers.values]
    Cache-Control = "public, max-age=31536000, immutable"

[[headers]]
  for = "/*.js"
  [headers.values]
    Cache-Control = "public, max-age=31536000, immutable"

[[headers]]
  for = "/*.css"
  [headers.values]
    Cache-Control = "public, max-age=31536000, immutable"
